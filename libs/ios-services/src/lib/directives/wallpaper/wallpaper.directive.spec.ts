import { WallpaperDirective } from './wallpaper.directive';

describe('WallpaperDirective', () => {
  it('should create an instance', () => {
    const directive = new WallpaperDirective();
    expect(directive).toBeTruthy();
  });
});
