import { SliderSlideDirective } from './slider-slide.directive';

describe('SliderSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderSlideDirective();
    expect(directive).toBeTruthy();
  });
});
