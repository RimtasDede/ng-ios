import { PressAniDirective } from './press-ani.directive';

describe('PressAniDirective', () => {
  it('should create an instance', () => {
    const directive = new PressAniDirective();
    expect(directive).toBeTruthy();
  });
});
