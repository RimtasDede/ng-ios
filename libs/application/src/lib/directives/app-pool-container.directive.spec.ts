import { AppPoolContainerDirective } from './app-pool-container.directive';

describe('AppPoolContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPoolContainerDirective();
    expect(directive).toBeTruthy();
  });
});
